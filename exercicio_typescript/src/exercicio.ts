const multiplicar = (valorA: number, valorB: number): number => valorA * valorB
const concatenar = (nome: string): string => `Olá ${nome}` 

console.log(multiplicar(10, 10))
console.log(concatenar("Jonatan"))